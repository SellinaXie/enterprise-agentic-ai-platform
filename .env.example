APP_NAME="Enterprise AI Transformation Advisor"
APP_ENV=local
APP_DEBUG=false
APP_LOG_LEVEL=INFO

# Required by assessment endpoints. Use a dedicated application database and account.
DATABASE_URL=postgresql+psycopg://enterprise_ai:change-me@localhost:5432/enterprise_ai

# Optional integration-test database. Its name must contain "test" and it must not point to
# DATABASE_URL. The PostgreSQL test suite resets the V2 schema and deletes its own rows.
TEST_DATABASE_URL=

# Required by POST /api/v1/assessments. The app and health endpoint start without it.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
OPENAI_TIMEOUT_SECONDS=30
OPENAI_MAX_RETRIES=2
# Keep provider-side response storage disabled by default for enterprise context.
OPENAI_STORE_RESPONSES=false

# Default execution guard for future LangGraph workflows.
LANGGRAPH_RECURSION_LIMIT=25
